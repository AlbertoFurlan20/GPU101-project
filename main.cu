#include "src/cuda-impl/cuda_implementation.cuh"

int main()
{
    // check_devices_status();

    // run_cuda_main();

    run_assignment_cuda(1);
    run_assignment_cuda(2);
    run_assignment_cuda(3);

    return 0;
}